import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
  edgeSwipeStrip: {
    position: 'absolute',
    left: 0,
    top: 0,
    bottom: 0,
    width: 28,
    zIndex: 100,
  },
});
